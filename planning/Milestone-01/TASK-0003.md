# TASK-0003 — Development Configuration

**Work Package:** WP-0001 Repository Foundation

**Status:** Planned

**Quality Gate:** Gate 1 — Structure

---

## Objective

Add local development entry-point files (placeholders only).

---

## Context

Provides `make` targets and Docker Compose skeleton before services are defined in WP-0002.

---

## Prerequisites

* TASK-0002 complete, reviewed, and committed

---

## Constraints

* Placeholder Makefile targets only — echo or document future WP
* Empty or minimal `docker-compose.yml` — no services until WP-0002
* No secrets in `.env.example`
* Do NOT install Symfony, React, or Python

---

## Deliverables

* `Makefile` — targets: up, down, test, lint, analyse, backend, frontend, worker
* `docker-compose.yml` — valid skeleton, `services: {}`
* `.env.example` — documented variables for Postgres, Redis, MinIO, backend, frontend, worker

---

## Acceptance Criteria

* [ ] `make help` lists targets
* [ ] `docker compose config` validates
* [ ] `.env.example` documents all planned service variables
* [ ] No running services configured yet
* [ ] No framework installed

---

## Out of Scope

* Docker service definitions → WP-0002
* GitHub Actions → TASK-0004 / WP-0006
* Application code

---

## Definition of Done

* Gate 1 passed
* Architecture Review approved
* Committed: `TASK-0003: add development configuration`
