# TASK-0070.6 — Runtime Intelligence Dashboard

## Phase

**Platform — AI Engine Infrastructure** (conclusion de la série Runtime 70.x)

## Status

**Complete**

## Objective

Transformer `/settings/runtime` en **Runtime Health Dashboard** — centre de contrôle explicable alimenté par des données réelles du Runtime (aucune simulation).

## Deliverables

| Slice | Deliverable |
| ----- | ----------- |
| P70.6-SLICE-01 | Domain `RuntimeDashboard` + score models |
| P70.6-SLICE-02 | `RuntimeScoreCalculator` + `PlatformScoreCalculator` |
| P70.6-SLICE-03 | `RuntimeDashboardAssembler` (capabilities, hardware, premium) |
| P70.6-SLICE-04 | Timeline depuis benchmarks, validations, exécutions |
| P70.6-SLICE-05 | API `GET /api/runtime/dashboard` |
| P70.6-SLICE-06 | UI `RuntimeHealthDashboard` (page d'accueil settings/runtime) |
| P70.6-SLICE-07 | Shadow commentary + docs + tests |

## API

```http
GET /api/runtime/dashboard
```

## Validation

```bash
make prod-rebuild
docker compose -f docker-compose.prod-like.yml exec backend php bin/phpunit --filter RuntimeDashboard
curl -s http://localhost:8000/api/runtime/dashboard | jq '.overallRuntimeScore.score'
```
