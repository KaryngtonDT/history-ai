# Learning Path

`LearningPathBuilder` constructs a curriculum from:

1. Fixed senior-developer template (DI → Messenger → DDD → CQRS → Docker → K8s)
2. Observed `KnowledgeItem` entries from Memory
3. Prerequisite gates (mastered concepts unlock next steps)

Objectives map `KnowledgeProgress` to `TeachingProgressStatus`.
