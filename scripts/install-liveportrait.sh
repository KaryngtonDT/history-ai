#!/usr/bin/env bash
# LivePortrait install stub — manual GPU setup required.
set -euo pipefail
MODELS_ROOT="${MODELS_ROOT:-/models}"
mkdir -p "${MODELS_ROOT}/liveportrait"
printf '[install-liveportrait] See docs/architecture/CAPABILITY_PLATFORM_VISION.md#lip-sync\n'
exit 0
