# Local development overrides (auto-merged with docker-compose.yml only).
# Not applied when using docker-compose.prod-like.yml (separate project: history-ai-prod-like).

name: history-ai

services:
  frontend:
    build:
      dockerfile: infrastructure/docker/frontend/Dockerfile.dev
    environment:
      VITE_USE_MOCK: ${VITE_USE_MOCK:-true}
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000}
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    command: npm run dev -- --host 0.0.0.0 --port 5173

volumes:
  frontend_node_modules:
    name: history-ai-frontend-node-modules
