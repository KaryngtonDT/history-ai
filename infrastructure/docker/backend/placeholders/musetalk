#!/usr/bin/env python3
"""MuseTalk CLI placeholder (legacy)."""
import argparse
import json
import os


def main() -> int:
    parser = argparse.ArgumentParser(description="MuseTalk placeholder")
    parser.add_argument("--video", required=False)
    parser.add_argument("--audio", required=False)
    parser.add_argument("--output", required=False)
    args = parser.parse_args()

    if args.output:
        os.makedirs(os.path.dirname(args.output) or ".", exist_ok=True)
        with open(args.output, "wb") as handle:
            handle.write(b"\x00\x00\x00\x18ftypmp42\x00\x00\x00\x00mp42isom")
        print(json.dumps({"output": args.output, "legacy": True}))
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
