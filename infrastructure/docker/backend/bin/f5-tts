#!/bin/bash
set -euo pipefail
VENV_PYTHON="/models/venvs/f5-tts/bin/python"
RUNNER="/opt/lumen/engines/f5_tts_runner.py"
if [[ -x "${VENV_PYTHON}" && -f "${RUNNER}" && -d "/models/f5" ]]; then
  exec "${VENV_PYTHON}" "${RUNNER}" "$@"
fi
exec /opt/lumen/placeholders/f5-tts "$@"
